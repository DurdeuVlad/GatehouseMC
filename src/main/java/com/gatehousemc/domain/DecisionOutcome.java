package com.gatehousemc.domain;

public enum DecisionOutcome {
    APPROVED,
    DENIED,
    BLOCKED,
    UNDONE,
    ALREADY_RESOLVED,
    ALREADY_PENDING,
    ACTIVE_REQUEST_EXISTS,
    RESOLVING,
    NOT_FOUND,
    FAILED
}
