package com.gatehousemc.domain;

public enum DecisionOutcome {
    APPROVED,
    DENIED,
    BLOCKED,
    UNDONE,
    ALREADY_RESOLVED,
    ALREADY_PENDING,
    CONFLICT,
    RESOLVING,
    NOT_FOUND,
    FAILED
}
